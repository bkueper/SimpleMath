<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simplemath">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleMath">
        <activity android:name=".Hochzaehlen"></activity>
        <activity android:name=".GroesserKleiner" />
        <activity android:name=".AuswertungZahlenEinfuegen" />
        <activity android:name=".ZahlenEinfuegen" />
        <activity android:name=".Spielauswahl" />
        <activity android:name=".UserErstellen" />
        <activity android:name=".Startseite" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>